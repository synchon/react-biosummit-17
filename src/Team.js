import React from 'react';

const Team = props => {
        return (
            <section className="hero">
                <div className="column is-fullwidth has-text-centered animated fadeIn">
                    Our Team
                </div>
            </section>
        )
};

export default Team;